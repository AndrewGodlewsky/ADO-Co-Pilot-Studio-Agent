# Iteration Paths — [PROJECT]

> **TEAM-AUTHORED · BLOCKING.** Replace every `<<placeholder>>`. See `_INTERVIEW.md` §3.2 (Project Settings → Project configuration → Iterations).

## Summary
This document defines the [PROJECT] **iteration paths** (sprints) the agent may assign work to, the **naming scheme**, and the **default** iteration policy when a request doesn't specify one. The agent uses this to validate the `Iteration` input and to schedule new items sensibly.

**Also known as:** iteration path, sprint, iteration, which sprint, schedule, backlog vs sprint, when does this go.

## Naming scheme
`<<Describe the iteration naming pattern, e.g. "[PROJECT]\\Sprint NN (yyyy-mm)">>`

## Active / current iterations

| Iteration path | Window | Status |
|---|---|---|
| `<<ITERATION_PATH_1>>` | `<<START–END>>` | `<<current / upcoming / past>>` |
| `<<ITERATION_PATH_2>>` | `<<START–END>>` | `<<current / upcoming>>` |

## Default iteration policy
When the user does not specify an iteration, use: **`<<DEFAULT_ITERATION_POLICY>>`** (e.g. "the team's backlog / unassigned" or "the current sprint").

## How far ahead the agent may assign
`<<e.g. "only the current and next sprint; further-out work goes to the backlog.">>`

*Verified <<DATE>> · review quarterly · owner: <<OWNER>>.*
