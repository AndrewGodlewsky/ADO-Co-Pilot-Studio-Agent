# Area Paths — [PROJECT]

> **TEAM-AUTHORED · BLOCKING.** Replace every `<<placeholder>>` with your real values. See `_INTERVIEW.md` §3.1 for the questions and where to find each answer in Azure DevOps (Project Settings → Project configuration → Areas).

## Summary
This document lists the valid **area paths** in the [PROJECT] project, the **default** area used when a request doesn't specify one, and which area maps to which team or component. The agent uses this both to place new items correctly and to validate the `AreaPath` input.

**Also known as:** area path, area, classification node, which area, team area, component area, where does this go.

## Valid area paths

| Area path | Maps to (team / component) | Notes |
|---|---|---|
| `<<AREA_PATH_1>>` | `<<TEAM_OR_COMPONENT_1>>` | `<<NOTES>>` |
| `<<AREA_PATH_2>>` | `<<TEAM_OR_COMPONENT_2>>` | `<<NOTES>>` |
| `<<AREA_PATH_3>>` | `<<TEAM_OR_COMPONENT_3>>` | `<<NOTES>>` |

## Default area path
When the user does not specify an area, use: **`<<DEFAULT_AREA_PATH>>`**.

## How an area is chosen
`<<Describe the rule, e.g. "match the named component; if none, ask; if the user declines, use the default above.">>`

*Verified <<DATE>> · review quarterly · owner: <<OWNER>>.*
