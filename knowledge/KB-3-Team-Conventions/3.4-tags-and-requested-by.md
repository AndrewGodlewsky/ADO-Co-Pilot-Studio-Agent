# Tags & the Requested-By Convention — [PROJECT]

> **TEAM-AUTHORED · BLOCKING.** Replace every `<<placeholder>>`. See `_INTERVIEW.md` §3.4. Relates to Architecture §4.10 (audit) and Open Item §7.4.

## Summary
This document defines the [PROJECT] **tag taxonomy** and the exact **`requested-by` stamp** the write flows apply to every item the agent creates or changes, so authorship is auditable even though the actual write runs as a service account.

**Also known as:** tags, labels, tag taxonomy, requested by, audit stamp, who asked, attribution, requested-by tag.

## Standard tags

| Tag | When to apply |
|---|---|
| `<<TAG_1>>` | `<<MEANING>>` |
| `<<TAG_2>>` | `<<MEANING>>` |

## The requested-by stamp
Every created or changed item is stamped with the requesting user. Exact format: **`<<REQUESTED_BY_FORMAT>>`** (e.g. `requested-by:user@org.com`). Applied as: `<<tag / description line / both>>`.

## Queryable RequestedBy field?
`<<Decide: tag/description line only, OR also a custom queryable field.>>` If a custom field is used, its reference name: `<<CUSTOM_REQUESTED_BY_REF>>`. (Architecture Open Item §7.4.)

*Verified <<DATE>> · review quarterly · owner: <<OWNER>>.*
