# Field Reference & Picklists — [PROJECT]

> **TEAM-AUTHORED · BLOCKING.** This is the single most important KB-3 doc — it makes writes land on the right fields. The reference names below are the **proposed defaults from Architecture §4.11**; **CONFIRM each against your process template** and fill the `<<placeholders>>` for custom fields. See `_INTERVIEW.md` §3.3 (Org Settings → Process → [your process] → [work item type] → Fields, or REST `GET {org}/{project}/_apis/wit/fields`). Closes Architecture Open Item §7.8.

## Summary
This document maps each work-item field the agent reads or writes to its exact **Azure DevOps reference name**, lists the **Value Area** picklist values, and records which optional fields are exposed on which type. The write flows and the child agent's input validation depend on these names being correct.

**Also known as:** field reference name, reference name, field id, System fields, VSTS fields, custom fields, value area, picklist, allowed values, release notes field, deploy notes field, feature flag field.

## Process template
This project uses the **`<<PROCESS_TEMPLATE>>`** process (Agile / Scrum / CMMI / custom inherited).

## Feature & User Story fields

| Field (team term) | Proposed reference name (CONFIRM) | At create | Notes |
|---|---|---|---|
| Title | `System.Title` | Required | — |
| Description | `System.Description` | Required | — |
| Acceptance Criteria | `Microsoft.VSTS.Common.AcceptanceCriteria` | Required | Confirm exposed on both types |
| Value Area | `Microsoft.VSTS.Common.ValueArea` | Required | Picklist below |
| Team | `<<TEAM_FIELD: derived from Area Path? or custom ref name>>` | Required | ADO has no default "Team" field — confirm |
| Story Points | `Microsoft.VSTS.Scheduling.StoryPoints` | Optional | Confirm exposed on Feature in your template |
| Priority | `Microsoft.VSTS.Common.Priority` | Optional | Typically 1–4 |
| Risk | `Microsoft.VSTS.Common.Risk` | Optional | e.g. 1-High / 2-Medium / 3-Low |
| Effort | `Microsoft.VSTS.Scheduling.Effort` | Optional | Confirm exposed on User Story |
| Discussion | work-item Comments (`System.History`) | Post-create | Via Comments API, not a field PATCH |
| Release notes | `<<CUSTOM_RELEASE_NOTES_REF>>` | Post-create | Custom field — supply reference name |
| Deploy notes | `<<CUSTOM_DEPLOY_NOTES_REF>>` | Post-create | Custom field — supply reference name |
| Feature-flag notes | `<<CUSTOM_FEATURE_FLAG_REF>>` | Post-create | Custom field — supply reference name |

## Task fields

| Field (team term) | Proposed reference name (CONFIRM) | At create | Notes |
|---|---|---|---|
| Title | `System.Title` | Required | — |
| Description | `System.Description` | Required | — |

## Value Area picklist
Allowed values: **`<<VALUE_AREA_VALUES>>`** (Microsoft default is `Business` / `Architectural`). How "enabler" maps: `<<e.g. enabler ≈ Architectural>>`.

## Exposure confirmations
- Story Points exposed on Feature? `<<yes / no>>`
- Effort exposed on User Story? `<<yes / no>>`

*Verified <<DATE>> · review quarterly · owner: <<OWNER>>.*
